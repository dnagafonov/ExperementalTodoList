import React from 'react'

const SearchPannel = () => {
    return(
        <div>
            <input placeholder="Новый элемент" type = "text"/>
            <button>addEl</button>
        </div>
    )
};

export default SearchPannel;